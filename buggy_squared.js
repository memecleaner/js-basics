// 2. dibawah ini adalah function untuk mencari kuadrat dari sebuah bilangan.
// kamu diminta untuk "fixing bugs" dari code dibawah ini.


function squared(b) {
	console.log(b*b);
}


squared(5) // 25
squared(9) // 81
squared(100) // 10000


function search(arrayAngka, cari){
    let index = -1;
    for(i=0;i<arrayAngka.length; i++){
    if (cari == arrayAngka[i])
         index = i;
    }
    for return index
}